# PM 세션

> **작업 세션은 이 파일을 읽지 마라.** PM 전용이다.
> 아래 "미기록 관찰"에는 작업 세션이 스스로 발견해야 할 내용이 들어 있다.
> 미리 읽으면 관찰이 아니라 받아쓰기가 된다.

## 새 PM 세션 개시 프롬프트 (복붙)

```
너는 Claro 프로젝트의 PM 세션이다. 구현은 하지 않는다.

읽을 것:
  CLAUDE.md
  docs/PM.md            ← 이 파일. 역할과 미기록 관찰
  docs/DECISIONS.md     ← 지금까지 정한 것 전부
  docs/development-*.md ← 각 레인 진행 상황
  logs/                 ← 완료된 작업 기록

docs/FINDINGS.md 는 지금 읽지 마라. 43KB다.
필요한 절만 그때그때 읽는다.

현재 위치는 docs/development-backend.md 맨 위에 적혀 있다.
```

## PM이 하는 일 / 안 하는 일

| 한다 | 안 한다 |
|---|---|
| Step 분할, 세션 개시 프롬프트 작성 | 구현 |
| 작업 세션 산출물 검수·분류 | 계약 **내용** 설계 |
| DECISIONS.md 관리 | 픽스처 작성 |
| "한 타입 한 파일" 정합성 감시 | 코드 리뷰 상세 |

**PM이 반복해서 저지른 실수**: 실물 없이 계약 내용을 설계한 뒤 작업 세션에
"확인할 사항"으로 넘기는 것. 그러면 세션이 PM 추론을 전제로 작업한다.
2026-09-20 에 두 번 발생(D8 `open_while`, D12 render). 추론이 생기면
**DECISIONS 에 OPEN 으로 내리고 작업 세션이 실물로 답하게 한다.**

**PM 프롬프트의 숨은 전제가 틀린 적**: S2 프롬프트에 "concept-library 문안을 먼저
가져다 쓴다"고 썼다. 라이브러리를 검증된 문안으로 취급한 것이다. 실제로는 라이브러리도
손으로 만든 산출물이고 자기 규칙을 두 군데서 어겼다(C-0002 ④ 현재형, C-0005 "전원").
S2 는 지시대로 옮겼고, 그 결과 골든 안에서 모순이 생겼다(2026-09-21).
→ **프롬프트에 "이 자료를 믿고 써라"를 넣을 때, 그 자료가 검증된 적 있는지 먼저 확인한다.**

## 인계 규칙

1. 판단을 대화에만 남기지 않는다. 문서에 없으면 사라진 것이다
2. PM 교체 전 아래 "미기록 관찰"을 갱신한다
3. 작업 세션 산출물을 받으면 **DECISIONS 승격 / 다음 세션 이관 / 폐기** 로 분류한다

---

# 미기록 관찰

PM 이 실물을 직접 읽고 확인한 것 중 아직 계약 문서가 없어 갈 곳이 없는 것.
해당 Step 이 끝나면 계약으로 옮기고 여기서 지운다.

## S1 (0.0a) — 소비 완료 2026-09-20

검증 기준은 `scripts/verify-observed.py` 와 `docs/development-backend.md` 로 옮겼다.
결과: **통과.** 블록 통합 없음, 텍스트 대조 불일치 0장.

**이 장치가 작동한 증거**: S1 은 PM.md 를 읽지 않고도 속도계 오류를 독립적으로 찾아냈다.
미리 주지 않는 방식을 유지할 근거가 생겼다.

**PM 관찰이 틀렸던 것**: 이전 PM.md 는 "숙련 전용 사실 13개"만 적어 사실상
`입문 ⊂ 숙련 + 개념` 을 가정했다. 틀렸다. 입문에만 있는 사실도 있다
("연준 자신도 물가가 2% 근처로 돌아오는 건 내년 말쯤").
**레벨은 필터가 아니라 별도 선택이다.** 실물이 PM 추론을 고친 두 번째 사례.

## S2 (0.0b) — 게이트 통과 2026-09-21, 반영 중

게이트 판정(도윤): Q1~Q4 PM 추천대로, Q5 통과("그 차" 유지).
S2 가 반영 중: C-0005 REFRESHER · C-0002 ④ 라이브러리 수정 → 골든·invalid 재생성, kicker ①②③.
반영 커밋이 나오면 0.0b 완료 처리하고 C-1(린트)을 연다.

**PM 검수에서 잡은 것**: C-0005 REFRESHER "참가자 전원"이 같은 문단의 "의장은 제출하지
않았다"(F20)와 모순. S2 는 라이브러리 문제로는 봤지만(범위 밖 #2) 자기 골든 안의 모순으로
연결하지 못했다. 문장 단위 검증으로는 안 걸리는 유형이다(FINDINGS §5.5 경고 그대로).
→ correction-log 첫 게이트 기록. 유형 "축약 변질". **첫 번째 자동화 가능 오류.**

## S3 / S4 에 넘길 관찰 — 설계 결정 아님. 관찰로만 전달한다

| 관찰 | 출처 | 행선지 |
|---|---|---|
| 이란 사실들은 FOMC 브리프가 아니라 **SL-iran-war 소속**이다. 골든이 참조해야 할 사실을 스토리라인이 직접 소유한다 | 도윤 · 2026-09-21 | S3 · S4 |
| **volatility 는 fact 가 아니라 문장 조각(span)에 붙는다.** F11 "2026년"은 STABLE 인데 본문 "올해"는 DERIVED. D8 의 값 3개는 유지되지만 붙는 위치가 D8 가정과 다르다 | S2 로그 · S3 입력 절 | S4 |
| `refs` 에 `DC-*`(Derived Claim)를 F 와 섞어 넣었다. 따로 둘지 | S2 | S3 · S4 |
| 문장 분할 규칙(`.?!` 뒤 공백, "1." 예외)이 `where` 경로 때문에 사실상 계약이 됐다 | S2 | S3 |
| `formula.op` 6개로 충분했다: days_inclusive · weeks · months · months_round · years · years_floor | S2 | S4 |
| kicker ①①② 충돌 — 같은 개념이 두 장에 걸치자 "개념 표시"와 "독자용 번호"가 부딪혔다 | 게이트 Q1 | S3 (FOMC-4 증거) |
| **D17**: open_question 은 슬라이드 사이의 독립 데이터로 정의 | 도윤 | S3 **제약** |

S2 가 만든 `_` 주석 필드 전체 목록은 `logs/backend/phase-0-step-0b.md` "S3 입력" 절에 있다.

## S3 / S4 배정
`docs/development-backend.md` 의 "Step 0.1/0.2 가 답해야 할 findings" 표 참조.
원문은 각 픽스처의 `_findings` 에 있다.

---

# 다음 PM 이 바로 할 것

1. S2 게이트 반영 커밋 확인 → 검증 재실행 → 0.0b 완료 처리 → push
2. C-1(concept-library 린트) 세션 열기 — S2 반영 **이후**
3. D14(블록 enum 개폐) — S3 전까지 도윤과 결정
4. S3 프롬프트 작성 — 위 "S3/S4 에 넘길 관찰" 표를 development-backend.md 로 옮기며 작성
