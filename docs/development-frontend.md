# development · frontend

## 현재 위치
**차단됨.** Step 0.0b (골든 픽스처) 완료 전까지 시작하지 않는다.

픽스처 없이 시작하면 상상으로 만들게 되고, 계약이 나온 뒤 갈아엎는다.
계약 문서(`ARTICLE_PACKAGE.md`)가 아니라 **픽스처**가 해제 조건이다. 픽스처가 곧 계약이다.

## 해제 후 첫 Step (미작성)
`fixtures/fomc-2026-09.article.json` 하나로 슬라이드 UI 렌더.
상세는 픽스처를 보고 쓴다.

## 이 레인의 규칙
- `docs/contract/ARTICLE_PACKAGE.md` 는 **읽기 전용**
- 바꿔야 할 게 보이면 `docs/development-backend.md` 하단 "계약 변경 요청"에 적는다
- 픽스처를 고치지 마라

## 계약 변경 요청 대기 중
없음
